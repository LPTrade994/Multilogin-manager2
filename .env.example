# ───── modalità Postgres (default) ─────
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/multilogin?schema=public

# ───── modalità Dev-lite (SQLite) ─────
# DATABASE_URL=file:./dev.db

PORT=3000
